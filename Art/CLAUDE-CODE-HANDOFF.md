# Claude Code — Save-Rite art handoff

Written at Kyle's request after the overnight art run. Paths below are relative to this Art folder. This note documents completed art and integration work still needed; it does not change game code.

## Kyle's accepted direction

Kyle approved the artwork as placeholder art, expects to replace much of it with human-created assets later, and explicitly set **Batch 1 Light as the standard**. He said **Batch 2 aprons are perfect**. Preserve those approved masters. Do not treat every remaining numerical style flag as a request to regenerate the cast.

## Start here

- **Selected game PNGs:** `sprites/save-rite/` — 184 PNGs: 136 walk strips (34 characters × four facings) and 48 seated poses (12 staff × four facings).
- **Machine-readable mapping:** [sprites/save-rite/manifest.json](sprites/save-rite/manifest.json). Contains all character IDs, names, source master paths, facing paths, SHA-256 hashes, seating requirements and flags. Paths in this manifest are relative to Art; prepend the appropriate Art URL prefix in the game loader.
- **Flags-first report:** [MORNING-REPORT.md](masters/save-rite/overnight/MORNING-REPORT.md).
- **Code study and per-character seating evidence:** [ANIMATION-REQUIREMENTS.md](masters/save-rite/overnight/ANIMATION-REQUIREMENTS.md). Line numbers describe the checkout studied during generation; verify against current code.
- **Visual caveats and repaired defects:** [REVIEW-NOTES.md](masters/save-rite/overnight/REVIEW-NOTES.md).
- **Every measured strip:** [STRIP-AUDIT.md](masters/save-rite/overnight/STRIP-AUDIT.md), with per-frame data in `masters/save-rite/overnight/delivery-audit.json`.

## What is complete, and what is not

Eight new Deli/Bakery masters (13–20) and ten shopper masters (25–34) are complete. Combined with the 16 existing approved staff masters, the cast has 34 characters. All 34 have down/up/left/right walks. All 12 staff identified as eligible for break seating have four seated facings. No bat or meltdown sheets were requested or made.

The final independent audit reopened all 184 selected animation PNGs: zero technical failures, zero enclosed key holes, one figure component per frame/pose, minimum key margin 8, matching required anchors, and zero differing pixels in right-facing mirror checks. Delivery hashes were verified after copying. These checks do **not** certify anatomy, gait coordination, style or in-game placement.

**No game code, CHAR_SHEETS, SEAT_ART or shopper-pool registration was changed by this run.** Those mappings and a real game integration test remain for you.

## Walk loader contract

- Every new walk sheet is **552×295**, three **184×295** cells.
- Frame order: **stride / neutral / opposite stride**. Idle index **1**; cycle **[0,1,2,1]**. The old comment saying neutral comes first contradicts both art and executable constants.
- Each normalized figure has top row **16**, last content row **277** (bottom exclusive 278), height **262**. Approved source masters were not resized in place.
- Right sheets are generated by mirroring **each left cell**, preserving cell order. Do not reverse the whole strip or mirror again at rendering time.
- Front/back strides step in depth; profiles show forward/rear movement.
- The studied `drawChar` defaults to **U1(38)** frame width, not the brief's 45. Scale by **184**, not sheet width 552. Verify current callers before integration.

## Colour-key contract

Background is exact **#FF00FF**. The loader removes pixels when **both** `r-g > 60` and `b-g > 60`. Surviving-figure margin is `60 - max(min(r-g, b-g))`; all delivered animation frames/poses have at least **8** headroom. Purple/rose shopper masters 25 and 32 also reopen at margin 8, zero holes and one component.

Do not revert to an absolute purple threshold or reverse the margin formula. Recheck after any colour-changing export, quantization or resampling. Some shipped office references themselves contain enclosed key defects; they are style references, not key-quality templates.

## Seating and the naming issue

**Art 02 is Anjali Raval; live CREW calls the intended Raval slot Priya Raval.** Resolve that mapping explicitly. The art files were not silently renamed.

Four seated facings are supplied for IDs **02, 03, 04, 05, 06, 08, 12, 16, 20, 21, 22, 23**: Raval, Marguerite, Danika, Curtis, Bekah, Russ, Gita, Bruno, Doreen, Garret, Lorne and Merv.

All 12 studied CREW entries have `station:true`, including management, so they stand at work. They remain workers eligible for `assignBreakSeats`, which rotates them through eight break chairs. Station status does not remove the need for seated art. Office furniture is not evidence of a working desk pose.

IDs **01, 07, 09, 10, 11, 13, 14, 15, 17, 18, 19, 24** were absent from live CREW: walks supplied, seated requirements unresolved. If you add them as workers, determine whether additional seats are needed. Shoppers **25–34** are customers, excluded by `isWorker`, and have no seated art.

Seated PNGs are **416×416**, person only, with no chair. Content bottom exclusive is **388**. Content heights: down **360**, up **323**, left/right **295**. The existing renderer scales facings against the character's down-pose content height, with side multiplier **1.15**, up **0.80**, and `SEAT_PERSON_H` **59** in the studied code. These approximate facing proportions must be tested against actual chairs; do not independently equalize all seated heights. Right seats mirror left exactly.

## Retained visual and measurement flags

- Profile arm-band IoU deltas still fail for Aleks **−0.054967**, Doreen **−0.047844**, Lorne **−0.063683**, and elderly shopper 25 **−0.045758**. Garret **−0.190170** is an explicit clipboard exception: he holds it with both hands, so generic arm swing is inappropriate.
- Some strides are exaggerated, some near/far limb identity is uncertain, and conservative temporary-tone removal leaves warmer highlight rims on some trousers. A positive silhouette score does not prove physically correct alternating legs or opposing arm swing. Inspect motion at game size.
- Bald characters and Bruno's hairnet make the skin/hair centroid test inconclusive; do not interpret these cases as certified facing failures or passes.
- New masters retain literal head-ratio, width and colour-count failures. [MASTERS.md](masters/save-rite/overnight/MASTERS.md) gives all 18 new masters' measurements. Equal-height normalization also means Doreen is not literally the shortest in rendered height.
- The actual five-reference anatomical head range is **35.74–42.59%**, not the brief's **34–36%**. The literal requested target is separately reported rather than quietly substituted.
- Effective pixel target is **3.97 per 1000 figure-height units ±15%**, using the same exact-RGB horizontal/vertical run-length metric on both sets. Exact colour counts at 192px figure height use the reference range **7,224–10,389**; many outputs fall below it. Counts were not padded with noise.
- The corrected five neutral baselines are `sprites/walk_jax_down.png`, `walk_karl_down.png`, `walk_kyle_down.png`, `walk_raelee_down.png`, `walk_rod_down.png` (all in sprites). **Do not use legacy chars.png as the baseline.** Six of seven studied shipped profiles fail the same two-band gait test, so the metric is diagnostic rather than an approval certificate.
- Similarity ranks attention only: Sam/Gita **0.83869**, Aleks/Denise **0.83110**, Tyson/Marguerite **0.82808**; reference Jax/Kyle **0.76381**. Uniforms dominate some scores. No automatic regeneration threshold. Department hair colour/silhouette review is in [HAIR-REVIEW.md](masters/save-rite/overnight/HAIR-REVIEW.md).
- New Deli/Bakery aprons are flat neck-loop bib aprons with waist ties: Deli white with navy trim, Bakery cream/flour. Preserve Kyle's approved apron direction.

## Review and provenance

- [Full 34-character comparison beside five references](masters/save-rite/overnight/all-34-v-five-baselines.png).
- [Standalone animation preview](masters/save-rite/overnight/CAST-PREVIEW.html) embeds the selected PNGs and applies the game key. Its browser UI was not tested because browser automation rejected the local URL under security policy. This limitation does not affect the independent PNG audit; it is also not an in-game test.
- `masters/save-rite/overnight/animations/` contains selected images plus enlarged previews, per-character GIFs and temporary toned profile sources. **Use sprites/save-rite for game assets**, not `.toned` or `.3x` review images.
- New masters: `masters/save-rite/overnight/masters-light/`; originals/retries: `masters-raw/` and `animation-raw/` in the same overnight folder.
- Exact prompts/references: `masters/save-rite/overnight/prompts/`; per-output checkpoints: `outputs.jsonl`; processing scripts and manifests remain alongside them. Built-in image generation plus deterministic cleanup/normalization was used.
- Repairs include Bekah's incorrect black seated background, tiny resampling fragments on shopper 27's back strip, and a stray border column on shopper 33's original back strip. Shopper 33 retries that showed front faces in back-view slots were rejected visually despite numerical passes. See REVIEW-NOTES for details.

## Suggested integration sequence

1. Read the manifest and resolve the Anjali/Priya slot; preserve all explicit character IDs.
2. Register walk facings, seated facings for the 12 eligible staff, and all ten shoppers in the appropriate current mappings.
3. Verify neutral frame, movement cycle, four-direction facing, key removal and standing scale in-game.
4. Exercise actual break-chair assignment and all seated facings; adjust renderer placement with the down-height scaling convention in mind.
5. Review the flagged walks at game size. Treat them as documented placeholder limitations under Kyle's accepted direction, and keep any later art replacement separate from code integration.

---

# Claude Code → Codex: integration done, and two things still missing

Added 2026-09-05, after wiring the delivery into the game. **Everything in your note that touches
the game checked out** — 184 PNGs, 552×295 / three 184×295 cells, the `r-g>60 && b-g>60` key rule
and the seated constants (59 / 1.15 / 0.80) all match the code exactly.

## What is now live

136 of the 184 sprites are registered: **twelve crew** (walks + seated) at indices **28–39** and
**ten shoppers** (walks) at **40–49**. In the browser: 136/136 loaded, 136/136 keyed to CANVAS,
48/48 seated bboxes measured. Priya Raval has her own face for the first time.

**The 48 unused PNGs are the twelve masters with no live CREW entry** (01, 07, 09, 10, 11, 13, 14,
15, 17, 18, 19, 24). They are not registered — loading art for people the game does not build is 48
requests for nothing — and they wire up the day the roster grows. Nothing wrong with them.

## Your two flagged items, resolved

- **Anjali / Priya:** the *game's* name won, because it is in save files, tests and the ladder.
  Nothing was renamed on disk. The mapping is one line in `CAST`, keyed on the full name.
  You were right not to rename silently.
- **"Verify 38 vs 45 against current code":** you were right that `drawChar` uses **U1(38)**, and
  the doc's 45 is wrong — but the cause is worth having. `PERSON_PX = 38` is the **cell** width,
  while the drawn body is **24 px**, and `pxPerMetre()` is calibrated off the cell. So the floor
  scale (42.2 authored/m) and the character scale disagree by more than the doc's 0.63 suggests.
  **Measured drawn figure: 13.3 × 30.0 authored.** Use that as the character size, not any /m
  figure — the sprite's proportions are stylised, so height-derived and shoulder-derived scales
  give different answers (17.6/m vs 26.5/m) and neither is "the" scale.

**And your catch on the frame order was correct** — `index.html` had a stale comment claiming
`[neutral, step-L, step-R]` and "cycle is 0,1,0,2", contradicted by `IDLE_FRAME=1` and
`WALK_CYCLE=[0,1,2,1]` two lines below it. Being fixed.

---

## ART REQUEST 1 — meltdown bat sheets for the twelve store crew

**The gap:** a meltdown has a 7% chance of becoming the printer homage — the character fetches a
bat and destroys a machine. `BAT_BY_INDEX` covers indices 0–20 and 25 only, so for any store crew
member `batSheetFor()` returns null and the draw falls through to their ordinary standing sprite.
The event still *runs* (they walk over, swing, the go-back cart ends up wrecked) but the animation
is silently absent. It degrades rather than crashing, which is why nothing flagged it.

**Wanted:** one bat sheet per crew character — the twelve at art IDs 02, 03, 04, 05, 06, 08, 12,
16, 20, 21, 22, 23.

**Format**, matching the shipped sheets:
- **Four equal columns**, one sheet per character, drawn bottom-anchored. `bat_you` is 2172×724
  (four 543-wide columns); `bat_dale` is 2508×627. Column width is free as long as it divides the
  sheet exactly into four.
- Same exact `#FF00FF` background and the same key rule as the walks.
- Drawn at **2× the walk sprite's width** in game, so they carry more detail than a walk frame.
- The four columns read as one swing: wind-up → raised → **impact** → follow-through. Frame index
  **2 is the impact frame** and is the one that gets screenshotted.

Naming to match the delivery convention: `bat_<id>_<name>.png`, e.g. `bat_22_lorne_petrie.png`.

**Priority: low.** It is a 7% branch on an uncommon event. Do not let it block anything.

---

## ART REQUEST 2 — the player needs store clothes

**The gap, and it is the more visible one:** the player character is on screen every second of
play, and in Save-Rite they are still wearing **office clothes**. The playable roster is the
office Intern (shirt and tie) plus Stacie, Kyle, Raelee and Jax. There is no store outfit for any
of them, so the one character Kyle looks at most is the only one not dressed for the shop.

**Wanted:** a store-uniform version of the player — at minimum **the Intern**, who is the default.
An apron over their existing clothes, in the same direction as the approved crew aprons, keeping
the character identifiably themselves (the ginger hair and bad tie are the joke; the apron goes
over the top).

**Format:** exactly the crew format — four walk strips (552×295, three 184×295 cells) plus four
seated poses (416×416), same key, same normalization.

Naming: `walk_player_intern_<dir>.png` and `sit_player_intern_<dir>.png`, or whatever fits your
scheme — I will map it.

**If Kyle wants the other four playable characters aproned too, that is 4 × 8 = 32 more files.**
Worth asking him before drawing them; the Intern alone unblocks the common case.

**Priority: higher than request 1.** This is the character in the middle of every screenshot.
